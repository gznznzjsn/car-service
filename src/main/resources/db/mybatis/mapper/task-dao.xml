<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gznznzjsn.carservice.dao.TaskDao">
    <resultMap id="TaskResultMap" type="Task" autoMapping="false">
        <id column="task_id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="duration" property="duration" jdbcType="INTEGER"/>
        <result column="cost_per_hour" property="costPerHour" jdbcType="NUMERIC"/>
        <result column="specializations_value" property="specialization" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="base_column_list">
        name, duration, cost_per_hour, value
    </sql>
    <select id="read" parameterType="Long" resultMap="TaskResultMap">
        SELECT
        <include refid="base_column_list"/>
        FROM tasks JOIN specializations USING (specialization_id)
        WHERE task_id = #{taskId};
    </select>
    <select id="readTasks" parameterType="Long" resultMap="TaskResultMap">
        SELECT tasks.task_id, name, duration, cost_per_hour, value
        FROM assignments_tasks
                 JOIN tasks USING (task_id)
                 JOIN specializations USING (specialization_id)
        WHERE assignment_id = #{assignmentId}
    </select>
</mapper>